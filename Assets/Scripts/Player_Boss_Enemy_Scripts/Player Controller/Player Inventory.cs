using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInventory : MonoBehaviour
{
    public bool hasbeneathDepthsKey = false;   // Tracks if the player has the key
    public bool hasDash = false;  // Tracks if the player has acquired the dash ability

    // You can add more flags for other abilities or items if needed in the future
}
